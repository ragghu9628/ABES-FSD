import fs, { write } from 'fs'
const writeFileSync=(pathd,ata)=>{
        try{
            fs.writeFileSync(path,data);
            console.log("Data has written")
        }
        catch(error){
console.log("unable to write the data");
        }
    }
const readFileSync=();

